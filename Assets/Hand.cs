using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hand : Bundle
{

    private void Awake()
    {
        
    }
    // Start is called before the first frame update
    //void Start()
    //{
    //    System.Random rand = new System.Random();
    //    for (int i = 0; i < rand.Next(6)+2; i++)
    //    {
    //        this.Add(Card.Create(rand.Next(1, 14), Card.suits[rand.Next(4)]));
    //    }
    //    SetDisplay(true);
    //}

}
